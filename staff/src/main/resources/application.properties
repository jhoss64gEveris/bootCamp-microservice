# MONGODB (MongoProperties)
spring.data.mongodb.host= localhost
spring.data.mongodb.port=27017
spring.data.mongodb.authentication-database=admin
spring.data.mongodb.username=hrosillo
spring.data.mongodb.password=admin
spring.data.mongodb.database=db_banco
#spring.data.mongodb.uri=mongodb://localhost/test
spring.data.mongo.repositories.enabled=true

logging.level.org.springframework.data.mongodb.core.ReactiveMongoTemplate=DEBUG

# if spring data repository support is enabled

# DATA RESET (RepositoryRestConfiguration})
#spring.data.rest.baseUri=foo\
# base URI against which the exporter should calculate its links
